// ----------- bitwise.asa ------------
// Contains functions for bitwise operations

// bin: converts the top of the stack to the binary representation and pushes the string to the stack
def bin:
end

// and: pushes the result of a bitwise "and" to the stack
def and:
end

// or: pushes the result of a bitwise "or" to the stack
def or:
end

// xor: pushes the result of a bitwise "xor" to the stack
def xor:
end
